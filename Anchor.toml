[toolchain]
package_manager = "yarn"
anchor_version = "0.31.1"

[features]
seeds = true
resolution = true
skip-lint = false

[programs.localnet]
chat = "GyNvHEC54adUfH4n4VfDG2emdo2PWdk1JSCw5fa84U2h"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
